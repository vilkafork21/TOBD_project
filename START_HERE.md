# 🚀 БЫСТРЫЙ СТАРТ ПРОЕКТА

## ✅ Все готово к запуску!

Тестовые данные созданы: `data/raw/sample_energy_data.csv` (218 строк)

---

## 📋 ШАГИ ДЛЯ ЗАПУСКА

### 1. Проверьте Docker Desktop
```bash
docker --version
docker-compose --version
```

### 2. Запустите проект
```bash
cd /Users/antonzyukov/Desktop/Tobd_project
docker-compose up -d
```

### 3. Дождитесь запуска (2-3 минуты)
```bash
docker-compose ps
```

Все контейнеры должны быть в статусе "Up" или "healthy".

### 4. Откройте интерфейсы

| Сервис | URL | Логин/Пароль |
|--------|-----|--------------|
| **Airflow** | http://localhost:8081 | `airflow` / `airflow` |
| **Streamlit** | http://localhost:8501 | - |

### 5. Запустите ETL пайплайн

1. Откройте http://localhost:8081
2. Войдите: `airflow` / `airflow`
3. Найдите DAG `energy_analytics_etl`
4. Включите DAG (переключите тумблер)
5. Нажмите **"Trigger DAG"**

### 6. Просмотрите результаты

После выполнения всех задач (5-7 минут) откройте:

**http://localhost:8501**

Выберите период: **01.01.2024 - 03.01.2024**

---

## 🛑 ОСТАНОВКА

```bash
# Остановка (данные сохраняются)
docker-compose down

# Полная очистка
docker-compose down -v
```

---

## 📊 ЧТО ВКЛЮЧЕНО

- Apache Airflow для оркестрации ETL
- PySpark (local mode) для обработки данных
- PostgreSQL для хранения (raw + analytics)
- Streamlit для визуализации

**Готово к запуску!** 🎉




