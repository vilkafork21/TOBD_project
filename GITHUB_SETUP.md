# 📦 Подключение проекта к GitHub

## ✅ Статус: Git репозиторий инициализирован, первый коммит создан

---

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `energy-analytics-bigdata` (или любое другое имя)
   - **Description**: `Big Data проект: Энергетическая аналитика с Apache Spark и Airflow`
   - **Public** / **Private**: выберите по желанию
   - ⚠️ **НЕ ставьте галочки** на "Initialize with README" (у вас уже есть файлы)
4. Нажмите **"Create repository"**

---

## Шаг 2: Подключите локальный репозиторий к GitHub

GitHub покажет инструкции. Выполните команды **из раздела "…or push an existing repository from the command line"**:

```bash
cd /Users/antonzyukov/Desktop/Tobd_project

# Замените USERNAME на ваш GitHub username и REPO-NAME на название репозитория
git remote add origin https://github.com/USERNAME/REPO-NAME.git

# Переименуем ветку в main (GitHub использует main вместо master)
git branch -M main

# Отправим код на GitHub
git push -u origin main
```

**Пример:**
```bash
git remote add origin https://github.com/antonzyukov/energy-analytics-bigdata.git
git branch -M main
git push -u origin main
```

---

## Шаг 3: Проверьте результат

Обновите страницу вашего репозитория на GitHub — там должны появиться все файлы проекта.

---

## 📊 Что загружено в Git

✅ **Загружено:**
- Весь код Python (ETL, Spark jobs, Streamlit)
- Docker конфигурация (docker-compose.yml, Dockerfiles)
- SQL схемы (init.sql)
- Документация (README, guides)
- Структура директорий

❌ **НЕ загружено** (правильно, через .gitignore):
- CSV данные (data/raw/*.csv)
- Обработанные данные (data/processed/*)
- Логи Airflow
- Python кэш
- Docker volumes

---

## 🔄 Последующие изменения

Если внесете изменения в код:

```bash
cd /Users/antonzyukov/Desktop/Tobd_project

# Посмотреть изменения
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

---

## 📝 Примеры хороших сообщений коммитов

```bash
git commit -m "Fix: исправлена ошибка в SQL запросе Streamlit"
git commit -m "Feature: добавлена агрегация по месяцам"
git commit -m "Docs: обновлена инструкция по запуску"
git commit -m "Refactor: оптимизирован Spark job transform"
```

---

## 🎓 Для защиты проекта

При защите проекта предоставьте:
1. **Ссылку на GitHub репозиторий**
2. **Презентацию** с архитектурой и результатами
3. **Демонстрацию** работающего дашборда

---

**Готово!** Проект готов к загрузке на GitHub. 🚀




