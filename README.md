# Энергетическая аналитика - Big Data проект

Проект по дисциплине «Технология обработки больших данных» - распределённая система анализа потребления электроэнергии.

## 📋 Описание проекта

Система реализует полный цикл работы с данными:
- **Сбор**: загрузка данных о потреблении электроэнергии из CSV файлов
- **Оркестрация**: управление ETL-процессами через Apache Airflow
- **Обработка**: распределённая обработка больших объёмов данных с Apache Spark
- **Хранение**: PostgreSQL для сырых данных и аналитического хранилища (DWH)
- **Визуализация**: интерактивный дашборд на Streamlit

## 🏗️ Архитектура системы

```
Источник данных (CSV файлы)
    ↓
Apache Airflow (Оркестрация ETL)
    ↓
Apache Spark (Обработка данных)
    ↓
PostgreSQL Raw (Сырые данные)
    ↓
PostgreSQL DWH (Агрегированные данные)
    ↓
Streamlit Dashboard (Визуализация)
```

## 🛠️ Технологический стек

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| Big Data обработка | Apache Spark | 3.5.x |
| Оркестрация ETL | Apache Airflow | 2.8.x |
| Хранилище | PostgreSQL | 15.x |
| Визуализация | Streamlit | 1.29.x |
| Контейнеризация | Docker + Docker Compose | 24.x |

## 📁 Структура проекта

```
energy_analytics/
├── data/
│   ├── raw/                    # Исходные CSV файлы
│   └── processed/              # Обработанные данные (Parquet)
├── airflow/
│   └── dags/                   # Airflow DAG файлы
│       └── energy_etl_dag.py
├── spark_jobs/
│   ├── extract.py              # Извлечение данных
│   ├── transform.py            # Трансформация и агрегация
│   └── load.py                 # Загрузка в DWH
├── streamlit_app/
│   └── app.py                  # Streamlit дашборд
├── sql/
│   └── init.sql                # Инициализация БД
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## 🚀 Быстрый старт

### Предварительные требования

1. **Docker Desktop** - [Скачать](https://www.docker.com/products/docker-desktop/)
2. **Git** (опционально) - для работы с репозиторием

### Установка и запуск

1. **Клонирование репозитория** (если используется Git):
```bash
git clone <repository-url>
cd energy_analytics
```

2. **Подготовка данных**:
   - Скачайте датасет [Smart meters in London](https://www.kaggle.com/datasets/jeanmidev/smart-meters-in-london) с Kaggle
   - Распакуйте CSV файлы в директорию `data/raw/`
   - Или создайте тестовый CSV файл с колонками: `LCLid`, `day`, `energy_kwh`, `timestamp`

3. **Запуск всех сервисов**:
```bash
docker-compose up -d
```

4. **Проверка статуса сервисов**:
```bash
docker-compose ps
```

### Доступ к сервисам

После запуска доступны следующие интерфейсы:

| Сервис | URL | Учетные данные |
|--------|-----|----------------|
| **Airflow Web UI** | http://localhost:8081 | username: `airflow`, password: `airflow` |
| **Spark Master UI** | http://localhost:8080 | - |
| **Streamlit Dashboard** | http://localhost:8501 | - |
| **PostgreSQL** | localhost:5432 | user: `postgres`, password: `postgres` |

## 📊 Использование

### 1. Запуск ETL пайплайна

1. Откройте Airflow Web UI: http://localhost:8081
2. Войдите с учетными данными (`airflow` / `airflow`)
3. Найдите DAG `energy_analytics_etl`
4. Включите DAG (переключите тумблер слева)
5. Запустите DAG вручную (кнопка "Play")

### 2. Просмотр результатов

Откройте Streamlit Dashboard: http://localhost:8501

Дашборд включает:
- **Обзор**: общая статистика и временные ряды
- **Почасовой анализ**: тепловая карта и среднее потребление по часам
- **Дневной анализ**: диапазоны потребления и статистика по дням недели
- **Статистика домохозяйств**: топ потребителей и распределение
- **Аномалии**: выявленные аномалии в потреблении

### 3. Работа с данными напрямую

Подключение к PostgreSQL:
```bash
docker exec -it energy_postgres psql -U postgres -d energy_analytics
```

Примеры запросов:
```sql
-- Просмотр сырых данных
SELECT COUNT(*) FROM raw.energy_consumption;

-- Агрегированные данные по дням
SELECT * FROM analytics.daily_consumption ORDER BY day DESC LIMIT 10;

-- Статистика по домохозяйствам
SELECT * FROM analytics.household_stats ORDER BY total_consumption DESC LIMIT 10;
```

## 🔧 ETL Pipeline

Процесс обработки данных состоит из трёх этапов:

### 1. Extract (Извлечение)
- Загрузка CSV файлов из `data/raw/`
- Валидация структуры данных
- Очистка от дубликатов и некорректных записей
- Сохранение в PostgreSQL (schema: `raw`)

### 2. Transform (Трансформация)
- Очистка данных (обработка пропусков, выбросов)
- Агрегация по временным периодам:
  - Почасовые данные
  - Дневные данные
  - Недельные данные
  - Месячные данные
- Расчёт статистики по домохозяйствам
- Выявление аномалий (метод Z-score, 3 sigma rule)

### 3. Load (Загрузка)
- Загрузка агрегированных данных в PostgreSQL DWH (schema: `analytics`)
- Создание индексов для оптимизации запросов

## 📈 Визуализация

Streamlit дашборд предоставляет:

1. **Обзорная панель**:
   - Общее количество домохозяйств
   - Суммарное потребление
   - Средние и пиковые значения
   - Временной ряд потребления

2. **Почасовой анализ**:
   - Тепловая карта потребления по часам и дням
   - Среднее потребление по часам дня

3. **Дневной анализ**:
   - Диапазоны потребления (min-max) с средним значением
   - Статистика по дням недели

4. **Статистика домохозяйств**:
   - Топ-10 потребителей
   - Распределение среднего дневного потребления

5. **Аномалии**:
   - Визуализация выявленных аномалий
   - Топ домохозяйств с наибольшим количеством аномалий

## 🐳 Docker команды

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка сервисов
docker-compose down

# Перезапуск конкретного сервиса
docker-compose restart airflow-webserver

# Просмотр логов конкретного сервиса
docker-compose logs -f spark-master

# Остановка и удаление всех данных
docker-compose down -v
```

## 🔍 Мониторинг и отладка

### Airflow
- Логи задач доступны в Airflow UI
- Также можно просмотреть через: `docker-compose logs airflow-scheduler`

### Spark
- Spark Master UI: http://localhost:8080
- Логи: `docker-compose logs spark-master`

### PostgreSQL
- Подключение: `docker exec -it energy_postgres psql -U postgres -d energy_analytics`
- Логи: `docker-compose logs postgres`

## 📝 Формат данных

### Входные данные (CSV)

Ожидаемые колонки:
- `LCLid` - идентификатор домохозяйства (строка)
- `day` - дата (формат: YYYY-MM-DD)
- `energy_kwh` - потребление электроэнергии в кВтч (число)
- `timestamp` - временная метка (формат: YYYY-MM-DD HH:MM:SS)

### Структура базы данных

**Raw схема** (`raw.energy_consumption`):
- Хранит сырые данные из CSV файлов

**Analytics схема**:
- `hourly_consumption` - почасовые агрегаты
- `daily_consumption` - дневные агрегаты
- `weekly_consumption` - недельные агрегаты
- `monthly_consumption` - месячные агрегаты
- `household_stats` - статистика по домохозяйствам
- `anomalies` - выявленные аномалии

## 🐛 Решение проблем

### Проблема: Airflow не запускается
```bash
# Проверьте логи
docker-compose logs airflow-init

# Пересоздайте контейнеры
docker-compose down -v
docker-compose up -d
```

### Проблема: Spark не может подключиться к PostgreSQL
- Убедитесь, что PostgreSQL запущен: `docker-compose ps postgres`
- Проверьте переменные окружения в docker-compose.yml

### Проблема: Нет данных в дашборде
- Убедитесь, что ETL пайплайн выполнен успешно в Airflow
- Проверьте наличие данных в БД: `SELECT COUNT(*) FROM analytics.daily_consumption;`

## 📚 Дополнительные ресурсы

- [Apache Spark Documentation](https://spark.apache.org/docs/latest/)
- [Apache Airflow Documentation](https://airflow.apache.org/docs/)
- [Streamlit Documentation](https://docs.streamlit.io/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

## 👥 Авторы

Проект выполнен в рамках дисциплины «Технология обработки больших данных»

## 📄 Лицензия

Этот проект создан в образовательных целях.

